name: career_naksha
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.2.0
  flutter_svg: ^1.0.0
  smooth_page_indicator: ^1.0.0
  connectivity_plus: ^2.3.9  # Ensure the latest stable version of connectivity_plus
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/gif1.png
    - assets/gif2.png
    - assets/git3.png
